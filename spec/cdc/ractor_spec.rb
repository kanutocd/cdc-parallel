# frozen_string_literal: true

RSpec.describe Cdc::Ractor do
  it "has a version number" do
    expect(Cdc::Ractor::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
